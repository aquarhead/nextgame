procs:
  api:
    cwd: api
    shell: "just dev"
    stop:
      send-keys: [ "<C-c>" ]
  web:
    cwd: web
    shell: "just dev"
    stop:
      send-keys: [ "<C-c>" ]
  open:
    shell: "open http://localhost:5173"
    autostart: false
